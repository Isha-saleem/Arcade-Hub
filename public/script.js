
document.addEventListener('DOMContentLoaded', () => {
    console.log('WaveCore hub loaded!');
    // Example: Add a simple hover effect with JS if needed (though Tailwind handles most)
    // const gameCards = document.querySelectorAll('.game-card');
    // gameCards.forEach(card => {
    //     card.addEventListener('mouseenter', () => {
    //         console.log('Mouse entered:', card.querySelector('h2').textContent);
    //     });
    //     card.addEventListener('mouseleave', () => {
    //         console.log('Mouse left:', card.querySelector('h2').textContent);
    //     });
    // });
});
